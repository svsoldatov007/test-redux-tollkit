// import { createSelector } from 'reselect'

export const charMassive = (state) => state.characters.charMassive

export const charactersStatus = (state) => state.characters.charactersStatus

export const characterActive = (state) => state.characters.characterActive
